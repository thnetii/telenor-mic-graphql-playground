import * as React from "react";

import { CounterProps } from "../../types/counter.types";

import AppPage from "../../components/apppage.component";

const CounterPage = (props: CounterProps) =>
  <AppPage />;

export default CounterPage;
