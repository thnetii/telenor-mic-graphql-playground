import * as React from "react";
import {
  Typography
} from "@material-ui/core";

import AppPage from "../../components/apppage.component";

const HomePage = () =>
  <AppPage>
    <Typography>Welcome to the Managed IoT GraphQL Playground!</Typography>
  </AppPage>;

export default HomePage;
