import * as React from "react";
import {
  Typography
} from "@material-ui/core";

const PageHeader = () =>
  <React.Fragment>
    <Typography variant="h4">Telenor Connexion Managed IoT Cloud</Typography>
    <Typography variant="subtitle1">Interactive GraphQL Playground</Typography>
  </React.Fragment>;

export default PageHeader;
